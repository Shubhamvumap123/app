## 2026-02-20 - ToggleGroup for Selection Grids
**Learning:** Selection interfaces (like time slots in `BookingModal`) are implemented as generic button grids, which lack semantic meaning and proper keyboard navigation.
**Action:** Replace button grids with `ToggleGroup` primitives to provide semantic labels, live region feedback, and arrow key navigation for improved accessibility.
